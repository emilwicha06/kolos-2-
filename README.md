# Rozwiązanie Zadania 1

Komendy wykonane po kolei:

```bash
1. docker run -d --name apache -p 8090:80 httpd
2. docker ps
3. (Otwarcie przeglądarki na localhost:8090)
4. docker logs apache
5. docker stop apache
6. docker rm apache
7. docker rmi httpd
```
